# 圖片素材需求清單（交給 AI 產圖用）

所有素材共通規格：

- 像素風（pixel art），畫面色調與 `assets/reference/phase1_tide_root_town_reference.png` 一致：
  暖木色、苔綠、樹皮深棕、琥珀燈光、藍綠色水面。
- **透明背景 PNG**，不要漸層背景、不要投影、不要浮水印。
- 圖片內每個物件請以**清楚的透明間隔**分開（間隔至少 16px），我會用透明區域自動切割。
- 不要使用任何既有遊戲的角色或商標。
- 產圖尺寸若無法剛好符合，請維持**整數倍**（例如 2×、4×），我會等比例縮回，不要非整數縮放。

優先級：**A = Phase 1 立即替換 placeholder**，**B = Phase 2 需要**，**C = Phase 3 以後**。

> **2026-09-04 進度**：A1～A10 已交付並套用（見 `docs/PHASE_2_REPORT.md` 第 2 節）。
> A11～A19 以合成參考表交付，已切割使用其中的虛空、雲霧、深色樹皮、樓梯；**過渡組（A11、A12）需重產**，規格見下方更新。
> B1、B2、B4、B5、B6 目前以程式合成 placeholder 運作中；B3、B10 已交付並套用；B7、B8、B9 尚未交付。
> **Phase 4（2026-09-04）**：CC、炸物魔王、投擲物、特效、三張行動表已切割套用；D1、D2 已由作者提供原圖補上；D3 已補上新版洞窟構圖參考。投擲物 v2 另提高一般幀主體尺寸，詳見 `docs/PHASE_4_VISUAL_POLISH.md`。
> **Phase 4.6（2026-09-05）**：四位主角 v2 行走表、待機表、兩張陰影、洞窟 32×32 tile 組已套用（見 `docs/PHASE_4_6_REPORT.md`）；D4 新造型行動表由作者提供原圖，已切割套用。
> **Phase 5（2026-09-05）**：城鎮更新 atlas、八個 v2 props、時段圖示、休息提示、日出表已套用（見 `docs/PHASE_5_REPORT.md`）；atlas 有格框與方向錯誤的格，本機已修正，正式版請見 E 節重出。

---

## A. 角色（Phase 1 替換合成幀）

目前四位角色的行走動畫是由單張站立圖合成的，需要真正的走路幀。

| 編號 | 內容 | 規格 |
|---|---|---|
| A1 | 哥哥：紅鬃山犬，紅圍巾，體格稍寬，領袖氣質 | 4 方向 × 4 幀行走 + 1 幀站立 |
| A2 | 冷靜哥：灰藍色貓頭鷹，圓眼鏡，小書袋 | 同上 |
| A3 | 妹妹：胖胖自然捲綿羊，食物斜背袋 | 同上 |
| A4 | 弟弟：溪水猴（藍髮、藍圍巾、長手長腳、尾巴水滴） | 同上 |

每位角色一張表：

```text
單格 48×64 px（建議產圖時放大 4 倍 → 192×256 每格，整張 768×1024）
列序（由上到下）：面向下（正面）、面向左、面向右、面向上（背面）
欄序（由左到右）：走路幀 1、2、3、4（第 1 幀即站立姿勢）
腳底對齊同一條基準線（距離格底 2px），角色寬度不超過 46px
```

四位角色高度請一致（約 56px），弟弟可以略矮一點但不要低於 46px。

## A. 主城道具（Phase 1 替換程式繪製的 placeholder）

| 編號 | 內容 | 建議尺寸 |
|---|---|---|
| A5 | **巨大樹心**：嵌在古樹樹幹中央、發出琥珀色光的樹心，周圍有年輪與細根纏繞 | 160×128 |
| A6 | **公告欄**：木框、兩根木樁、貼著 2～3 張紙條 | 48×48 |
| A7 | **雲端樹冠封鎖門**：藤蔓纏繞的木柵門，橫向，象徵「未開放」 | 128×48 |
| A8 | **旗幟**：木桿加藍綠色三角旗（廣場裝飾） | 20×48（2 款顏色） |
| A9 | **船港停靠處**：空的木造泊位＋「暫無船隻」的繩索與浮標（不要船） | 96×64 |
| A10 | **木柵欄**：橫向與直向各一款（封鎖左橋頭與右船港） | 64×48、32×64 |

## A. Tileset 補件（32×32 每格）

目前 tileset 已有：草地、草崖、泥土、根泥、樹皮、亮樹皮、樹根、木板、拼木、石板、苔石、
淺水、中水、深水、沙灘、草水岸、藤蔓、苔蘚、花草、蘑菇、岩石、樹樁、荷葉、蘆葦。缺的是：

| 編號 | 內容 |
|---|---|
| A11 | **草地邊緣過渡組（需重產）**：草地對泥土、草地對石板的 8 方向邊緣（上下左右＋四角）＋內角 4 格，共 16 格。**必須是正上方視角、每格恰好 32×32、格與格之間留 8px 透明間隔並排成 4×4**；上次交付為斜視角 72×80 的立體格，無法對應 autotile |
| A12 | **水岸過渡組（需重產）**：草地對淺水的 8 方向邊緣＋內角，共 16 格，規格同 A11 |
| A13 | **樹皮牆組**：牆頂、牆面、牆底陰影、左右邊、四角，共 12 格（中層外牆用） |
| A14 | **深色樹皮牆組**：同 A13 但更暗、帶少許雲霧（上層外牆用），12 格 |
| A15 | **木造樓梯**：向上樓梯 3 格（頂、中、底），加左右扶手各 1 格 |
| A16 | **樹枝木道**：樹枝表面木道、左右邊緣（露出樹皮）、末端，共 6 格 |
| A17 | **虛空／雲霧**：上層腳下的深藍夜空 2 款、雲霧 4 款、雲霧邊緣 4 格 |
| A18 | **橋面**：橋面木板、上緣欄杆、下緣欄杆、橋頭柱 4 格 |
| A19 | **廣場中心圖案**：4×4 格（128×128）的圓形年輪石板拼花，角色可走在上面 |

## B. Phase 2：互動與城鎮生命

| 編號 | 內容 | 規格 |
|---|---|---|
| B1 | **對話框**：木框＋羊皮紙底的九宮格框（可拉伸），含名字牌小框 | 九宮格 48×48，邊框 8px（程式 placeholder 使用中，可直接覆蓋 `assets/ui/dialogue_frame.png`） |
| B2 | **互動提示圖示**：驚嘆號、對話泡泡、問號、手指（浮在頭上） | 各 18×20，2 幀閃爍（placeholder：`assets/ui/interact_prompt.png` 的「E」泡泡） |
| B3 | **四位角色頭像**：對話框用，正面半身 | 各 48×48 |
| B4 | **燈籠動畫**：燈火閃爍 4 幀 | 44×48 × 4 橫排（placeholder：程式調亮度的 `assets/props/lamp_post.png`） |
| B5 | **水面動畫 tile**：淺水、深水、中水各 4 幀 | 32×32 × 4 × 3 橫排（placeholder：tileset 第 3 列的程式亮紋） |
| B6 | **樹葉飄落／蝴蝶／螢火蟲**粒子小圖 | 葉 6×6、螢火蟲 5×5、蝴蝶 7×7 × 2 幀（placeholder 在 `assets/effects/`） |
| B7 | **雲朵**：4 款不同形狀（上層飄動用，目前的可沿用但要更柔和） | 64～128 寬 |
| B8 | **日夜色調參考**：同一張主城參考圖的黃昏與夜晚版本（只做色調參考，不切割） | 任意 |
| B9 | **可互動物件**：可推木箱、可撿樹果、水池投幣、風鈴 | 各 32×32，2 幀 |
| B10 | **NPC 兩位**（父親原型：壯碩海獺漁夫；市集老闆：老龜） | 各 48×64，4 方向 × 4 幀 |

## C. Phase 3 以後（先不用產）

- 寵物 3～4 種（32×32，4 方向 × 4 幀）與親密度愛心圖示。
- 小怪 2～3 種、攻擊特效、受傷閃爍、HP 條。
- 平原、森林、海底根系洞窟、雲端樹冠的 tileset 與參考構圖。

---

## 交付方式

- 每個編號一張 PNG，檔名用編號＋英文，例如 `A1_big_brother_sheet.png`、`A11_grass_edges.png`。
- 放到 `assets/reference/incoming/`，我會用 `tools/build_assets.py` 切割並驗證尺寸。
- 角色表與 tileset 若能提供「格線版」與「無格線版」各一張最好，格線版只用來對位。

## D. Phase 4：需重新交付（檔案損毀）

以下三個檔案在分支 `codex/phase-4-cc-fried-food-battle` 上的內容不是 PNG（前 8 bytes 為 `59 aa e7 8a 78 2d ae e9`，
三個檔案大小都是 90059～90060 bytes，疑似同一次上傳出錯）。請重新輸出並確認能用一般看圖軟體開啟。

| 編號 | 檔案 | 目前處理 | 規格 |
|---|---|---|---|
| D1 | `ACTION_sister_carry_throw_reference.png` | **已補上**（作者 Downloads 原圖 1086×1448），已切割套用 | 與其他三張行動表相同：4 方向（下、右、左、上）× 4 欄（舉物、舉物、投擲、舉物），透明背景 |
| D2 | `GRANDMA_turtle_breakfast_sheet_reference.png` | **已補上**（原圖 1224×1285）。注意它不是行走表：四列相同、每列為五種視角；已重組為四方向站立幀。若之後需要阿嬤走動，請另交 5 欄 × 4 列的行走表 | 5 欄 × 4 列（4 行走幀 + 1 站立），圍裙、格紋頭巾、念珠、早餐籃 |
| D3 | `CAVE_fried_food_demon_arena_reference_v2.png` | **已補上構圖參考**；Phase 4.6 再交付 `assets/tiles/fried_food_cave_tiles_32.png`（4×2 正式 tile 組），已套用進遊戲 | 兩款地面最右一欄偏暗，切割器已補平；若日後重出，請讓地面 tile 四邊可無縫平鋪 |
| D4 | `ACTION_<id>_v2_carry_throw_reference.png` ×4 | **已由作者提供原圖補上**（4 列 × 3 欄，白底／棋盤格／透明底皆可；切割器泛洪去背、依 v2 站立幀高度縮放） | 第 0 欄舉物、第 1 欄投擲；投擲幀伸手超過 48px 的部分會被置中裁掉幾個像素 |


## E. Phase 5：城鎮更新 atlas 需修正（已本機修正，重出時請照下列規格）

`assets/tilesets/town_visual_refresh_tiles_32.png`（256×128，8×4）目前由 `tools/build_assets_phase5.py` 修正後才進遊戲。若重新輸出，請：

| 編號 | 問題 | 規格 |
|---|---|---|
| E1 | 每格最外圈 1px 偏亮、次外圈 1px 偏暗（像有格框），平鋪出格線 | 每格 32×32 的四邊要能與同材質無縫平鋪：外圈像素亮度與內部差 ≤ 10；不要畫格線、外框或每格獨立的陰影 |
| E2 | `path_edge_s`（第 1 列第 1 格）、`shore_edge_s`（第 2 列第 1 格）與 `_n` 同向（草都在上） | `_s` 應為「材質在上、草在下」：石板／水佔上方約 2/3，草與土坡在下緣 |
| E3 | `path_corner_sw`（第 1 列第 6 格）是草地佔四分之三的內凹角；`path_corner_se`、`shore_corner_sw`、`shore_corner_se` 與 `_nw`／`_ne` 同向 | 四個 corner 都是「石板／水佔大半，草只在指定的一個角」：nw 草在左上、ne 右上、sw 左下、se 右下。若要另外提供內凹角（草只在一個角、材質佔大半的反向版），請放在新的一列並在 manifest 註明 |
| E4 | `bridge_planks`／`bridge_edge` 是南北向（欄杆在左右） | 主城港口橋是東西向兩列：請另給「上列（欄杆在上）」與「下列（欄杆在下）」各一格；目前以旋轉合成 |
| E5 | `TOWN_visual_refresh_tiles_reference_v1.png` 不是 PNG（前 8 bytes 非 PNG 簽名） | 請重新輸出並用一般看圖軟體確認可開啟 |
| E6 | 共享家庭屋外觀 176×162 比原本的門口空間（3 列 = 96px）高，屋頂蓋到街道 | 若作者希望不遮街道：另給約 176×96～176×110 的版本，或接受目前效果（見 `docs/PHASE_5_REPORT.md` 第 6 節） |

八個 v2 props（透明背景、底部接地）規格正確，直接使用；`day_phase_icons.png` 四格順序請確認是「早晨、白天、黃昏、夜晚」。
